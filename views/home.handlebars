<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Black and White Spa</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous"></head>
    <link rel="stylesheet" href="/assets/style.css">
<body>
    <header class="container my-3 text-light">
        <h1>Black and White Spa</h1>
    </header>
    <form action="/procesar" method="POST" class="container">
    <div class="mb-3 text-light col-md-6">
        <label for="imagenUrl">Imagen URL:</label>
        <input type="text" class="form-control" id="imagenUrl" name="ruta">
    </div>
        <button type="submit" class="btn btn-outline-light">Subir imagen al servidor</button>
    </form>

    {{#if imagenUrl}}
        <div class="container mt-5">
            <h2 class="text-light">Imagen procesada:</h2>
            <img src="{{imagenUrl}}" class="img-fluid text-light" alt="Imagen procesada">
        </div>
    {{/if}}
</body>
</html>